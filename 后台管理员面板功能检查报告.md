# 后台管理员面板功能检查报告

检查日期：2026-05-20  
项目：PartsPro Italia 批发网站  
检查方式：代码审查 + 管理后台路由/API 梳理 + 本地静态检查与生产构建  
结论：后台主框架已经成型，覆盖商品、库存、订单、客户、B2B、RMA、系统健康等核心模块；上线前最需要补强的是管理员安全边界、订单库存事务一致性、操作审计、支付/发货/RMA 的业务凭证闭环。

## 1. 检查范围

### 后台页面

- `src/app/[locale]/admin/page.tsx`：后台总览
- `src/app/[locale]/admin/layout.tsx`：管理员权限保护与左右布局
- `src/app/[locale]/admin/products/page.tsx`：商品列表、翻译、发布、归档、批量操作
- `src/app/[locale]/admin/products/[skuId]/page.tsx`：商品详情编辑
- `src/app/[locale]/admin/inventory/page.tsx`：库存、采购导入、到货确认、库存设置
- `src/app/[locale]/admin/orders/page.tsx`：订单列表、状态筛选
- `src/app/[locale]/admin/orders/[orderId]/page.tsx`：订单详情、收款、备货、发货、取消、锁库延期
- `src/app/[locale]/admin/customers/page.tsx`：客户 CRM 列表
- `src/app/[locale]/admin/customers/[customerId]/page.tsx`：客户详情、备注、标签、任务、价格组
- `src/app/[locale]/admin/b2b/page.tsx`：B2B 审核
- `src/app/[locale]/admin/rma/page.tsx`：RMA 列表
- `src/app/[locale]/admin/rma/[rmaId]/page.tsx`：RMA 详情与状态流转
- `src/app/[locale]/admin/system/page.tsx`：系统健康检查

### 后台 API

- 商品：`/api/admin/products/*`
- 目录：`/api/admin/catalog/*`
- 库存：`/api/admin/inventory/*`
- 订单：`/api/admin/orders/*`
- 客户：`/api/admin/customers/*`
- B2B：`/api/admin/b2b/status`
- RMA：`/api/admin/rma/status`
- 系统：`/api/admin/health`

## 2. 当前已完成能力

| 模块 | 当前能力 | 完成度 |
| --- | --- | --- |
| 管理员入口 | `/zh/admin`、`/it/admin`，普通用户访问后台会跳回账户中心 | 高 |
| 左右后台布局 | 左侧菜单 + 右侧内容区，桌面/移动端均有布局 | 高 |
| 商品管理 | 商品列表、详情编辑、中文名批量生成、手动翻译、发布/归档、批量操作 | 中高 |
| 商品参数 | 支持参数定义、SKU 参数值、目录筛选参数 | 中 |
| Excel 导入 | 支持供应商 Excel 导入为采购/在途库存 | 中高 |
| 库存管理 | 库存设置、到货确认、缺货标记、手动调整、库存流水 | 中高 |
| 订单管理 | 订单列表、详情、现金/转账确认、备货、预购分配、发货/自提、完成、取消释放锁库 | 中高 |
| 支付方式 | Stripe、现金、银行转账三种方式的基础流程已接入 | 中 |
| 客户 CRM | 客户列表、客户详情、价格组、备注、标签、任务 | 中 |
| B2B 审核 | B2B 申请列表、状态审核、价格组写入 | 中高 |
| RMA 管理 | RMA 列表、详情、状态流转 | 中 |
| 系统健康 | 检查环境变量、Supabase 表可访问性、Stripe 配置状态 | 中 |

## 3. 上线前必须优先完善

### P0-1：`/api/admin/health` 目前没有管理员鉴权

证据：`src/app/api/admin/health/route.ts:6-8` 直接返回 `getSystemHealth()`，没有调用 `assertAdmin()`。

风险：虽然接口不直接返回密钥值，但会暴露哪些环境变量已配置、Supabase/Stripe 集成状态、后台表可访问情况。这个接口应只给管理员或内部运维使用。

建议：

- 在 `GET()` 内加入 `assertAdmin()`。
- 非管理员返回 `401/403`。
- 如需给 Vercel Cron 或外部监控使用，另建只返回 `ok/status` 的轻量接口，并使用 `CRON_SECRET` 或专用 token。

### P0-2：订单创建与库存锁定不是数据库事务，存在“幽灵锁库”风险

证据：

- `src/app/api/orders/route.ts:90-142` 先插入订单和订单项，再调用 `reserveSupabaseInventory()`。
- `src/app/api/orders/route.ts:139-142` 如果库存锁定失败，只删除订单，没有释放已成功锁定的前面几行库存。
- `src/lib/order-workflow.ts:209-257` 逐行更新 `inventory.stock_reserved/incoming_reserved`。

风险：多 SKU 订单中，如果第一行锁库成功、第二行失败，订单会被删除，但第一行库存可能已经保留为 reserved，造成库存被错误占用。

建议：

- 新增 Postgres RPC，例如 `create_order_with_reservations()`。
- 在数据库函数中使用事务和行锁处理订单、订单项、库存、库存流水。
- 函数失败时整体回滚，避免订单和库存状态不一致。

### P0-3：发货、自提、预购分配、取消释放也不是事务

证据：

- `src/lib/order-workflow.ts:260-338` 释放库存逐行处理，最后更新订单状态。
- `src/lib/order-workflow.ts:407-489` 预购分配逐行更新库存和订单项。
- `src/lib/order-workflow.ts:492-564` 发货/自提逐行扣库存，最后更新订单状态。

风险：如果中途某个 SKU 库存不足、网络失败或 Supabase 返回错误，可能出现部分商品已扣库存、订单状态未更新，或部分锁库释放、订单仍未取消。

建议：

- 将以下操作改为数据库 RPC：
  - `release_order_reservations()`
  - `allocate_preorder_stock()`
  - `ship_or_pickup_order()`
  - `complete_order()`
- 在 RPC 内校验状态、库存、支付状态，并一次性写入库存流水。

### P0-4：缺少管理员操作审计日志

当前状态：后台已经能改价格、库存、订单状态、收款状态、客户价格组、B2B 审核状态，但没有统一记录“谁在什么时候改了什么”。

风险：真实上线后遇到库存错误、现金收款争议、订单取消、价格改动时，很难追踪责任和恢复数据。

建议新增表：

- `admin_activity_logs`
- 字段建议：`id`、`actor_profile_id`、`actor_email`、`action`、`entity_type`、`entity_id`、`before_data`、`after_data`、`ip_address`、`user_agent`、`created_at`

优先记录：

- 确认现金/转账收款
- 取消订单和释放库存
- 发货/自提完成
- 库存调整
- 到货确认/缺货标记
- 商品价格修改
- 客户价格组修改
- B2B 审核通过/拒绝

## 4. 中优先级完善建议

### P1-1：商品发布/归档/更新的 `returnTo` 需要限制为站内后台路径

证据：

- `src/app/api/admin/products/update/route.ts:21-22`
- `src/app/api/admin/products/publish/route.ts:16-17`
- `src/app/api/admin/products/archive/route.ts:16-17`

当前代码直接用表单里的 `returnTo` 创建 `new URL(returnTo, request.url)`。如果管理员被诱导提交恶意表单，可能跳转到外部地址。

建议：

- 只允许 `returnTo` 以 `/zh/admin/` 或 `/it/admin/` 开头。
- 不符合规则时回退到 `/${locale}/admin/products`。
- 订单/RMA 部分已有更严格的回跳思路，可抽成通用 helper。

### P1-2：管理员 POST 表单建议增加 CSRF 防护

当前状态：后台大量操作使用普通 POST 表单，依赖 Supabase cookie 和浏览器 SameSite 策略。SameSite 可以挡住大部分跨站提交，但管理员面板属于高价值后台，建议再加 nonce/token。

建议：

- 后台 layout 生成 CSRF token。
- 写入 HttpOnly/SameSite cookie 或 server session。
- 所有 `/api/admin/*` mutation 校验 token。

### P1-3：顶部全局搜索是 UI，占位但未实现

证据：`src/components/admin/admin-shell.tsx:125-135` 有搜索输入，但没有提交、快捷键或查询结果逻辑。

建议：

- 第一版实现最小全局搜索：订单号、SKU、EAN、客户邮箱、客户公司、RMA 编号。
- 搜索结果跳转到对应详情页。
- 如果暂时不做，应隐藏搜索框，避免管理员以为功能可用。

### P1-4：列表页需要分页、排序和搜索，否则数据量上来会丢记录

当前状态：多处后台数据读取使用固定 limit，例如商品 300、订单 100、RMA 100、B2B 100。短期够用，但真实运营后会看不到更早数据。

建议：

- 所有列表页统一支持 `q`、`status`、`page`、`sort`。
- 后端查询使用 Supabase range 分页。
- 商品页增加 SKU/EAN/名称搜索。
- 订单页增加订单号、客户、支付状态、履约状态、日期范围。

### P1-5：订单状态流转需要更强的业务守卫

当前状态：后台订单详情提供多个操作按钮，但部分动作没有在 UI 层禁用，服务端也需要更严格校验。

建议规则：

- 未付款订单默认不能直接完成。
- 预购未分配到货前不能发货/自提。
- 已取消订单不能再次确认收款。
- 已完成订单不能再次释放锁库。
- 现金到付如果允许未付款发货，需要显式标记为 `cash_on_delivery`，不要和普通现金待收款混用。

### P1-6：现金和银行转账缺少凭证字段

当前状态：管理员可以确认现金/转账到账，但没有收款金额、凭证号、截图、到账时间、收款人等字段。

建议补充：

- `payments` 或 `order_payments` 表。
- 字段：`method`、`amount`、`currency`、`reference`、`proof_url`、`received_at`、`confirmed_by`、`note`。
- 订单只保存汇总状态，支付记录保存明细。

### P1-7：RMA 需要从“状态流转”升级为“售后处理闭环”

当前状态：RMA 有列表、详情、状态更新，但缺少实操字段。

建议增加：

- 退货授权号 RMA number
- 客户上传附件预览/下载
- 收到退货日期
- 检测结果
- 处理方案：退款、换货、维修、拒绝
- 退款金额或换货 SKU
- 客户沟通记录
- 关闭原因

### P1-8：商品和库存写入也需要事务化

当前状态：

- 商品创建/更新涉及 `products`、`skus`、`inventory`、`sku_attribute_values` 多表写入。
- Excel 导入和到货确认涉及采购单、库存、流水多表写入。

建议：

- 高风险写入放到 Postgres RPC 中。
- 或至少在失败时执行补偿清理。
- 库存调整必须写入审计日志和 inventory movement，且禁止手动调出负库存。

## 5. 低优先级但能明显提升运营效率

### P2-1：后台通知中心

当前顶部有通知/消息按钮，但没有实际内容。建议接入：

- 新 B2B 申请
- 新订单
- 待确认转账
- 库存低于补货线
- 预购到货待分配
- 新 RMA

### P2-2：客户 CRM 需要编辑/删除/合并能力

当前客户详情可新增备注、标签、任务，但缺少：

- 编辑/删除备注
- 删除标签
- 编辑任务截止时间
- 合并重复客户或把 B2B 申请转为正式客户
- 客户导出

### P2-3：库存模块增加扫描和报表

建议：

- EAN/SKU 扫码入库
- 采购单详情页
- 供应商字段与供应商管理
- 缺货/补货建议导出
- 库存流水 CSV/XLSX 导出

### P2-4：后台指标增加日期范围和口径说明

当前 dashboard 指标有业务价值，但需要更明确：

- 今日、7 天、30 天、本月
- 已付款收入 vs 所有订单金额
- 待付款金额
- 已取消金额
- 毛利估算：`销售价 - 成本价`

### P2-5：后台多角色权限

当前权限基本是 `admin` 和普通用户。后续建议拆分：

- `owner`：全权限
- `finance`：确认收款、退款、财务报表
- `warehouse`：库存、入库、发货
- `support`：RMA、客户备注
- `catalog_manager`：商品、价格、翻译

## 6. 建议实施路线

### 第一阶段：上线前安全与一致性

状态建议：必须完成后再开放真实订单。

- 保护 `/api/admin/health`
- 修正商品相关 `returnTo` 跳转校验
- 增加管理员操作日志
- 将订单创建/锁库/取消/发货/预购分配改为数据库事务 RPC
- 管理员关键 POST 增加 CSRF token
- 订单状态动作增加服务端状态校验

### 第二阶段：运营闭环

- 现金/转账凭证记录
- RMA 退货处理字段和附件查看
- 订单详情增加付款记录、发货记录、内部备注时间线
- 列表分页、搜索、排序
- 后台通知中心

### 第三阶段：效率和规模化

- 扫码入库/发货
- 库存和订单导出
- 客户 CRM 合并和自动跟进
- 多管理员角色权限
- 后台指标日期范围与毛利分析

## 7. 验证结果

本地检查结果：

- `npm run lint`：通过
- `npm run typecheck`：通过
- `npm run build`：通过

构建结果确认：

- 后台页面均被 Next.js 识别为动态路由。
- 后台 API 路由均成功编译。
- 当前检查未执行线上真实订单、真实付款、真实库存扣减。

## 8. 总结

后台已经具备 MVP 运营骨架：管理员能进入后台、管理商品、库存、订单、客户、B2B 和 RMA，也能处理现金、转账、Stripe 订单的基础状态。  

但如果目标是“真实上线并接单”，最优先不是继续加页面，而是把关键写入流程做硬：权限不漏、库存不乱、订单状态不可越权、每个管理员操作可追踪。完成第一阶段后，系统会从“可演示的后台”升级为“可以放心试运营的后台”。
