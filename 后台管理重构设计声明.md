# PartsPro 后台管理重构设计声明

本文档是后台重构的总入口。当前执行方案已经切换为：真实 React Ant Design、中文后台优先、多 MD 文档拆分、直接替换旧 `/admin`、按模块逐步完成全链路重写。

## 文档索引

- [00-总规划](docs/admin-rebuild/00-总规划.md)：目标、阶段、执行顺序、删除策略。
- [01-UI设计系统](docs/admin-rebuild/01-UI设计系统.md)：Ant Design 主题、布局、表单、表格、筛选、弹窗、状态反馈规范。
- [02-业务状态机](docs/admin-rebuild/02-业务状态机.md)：订单、付款、库存、客户、退款状态与允许动作。
- [03-数据与API重写](docs/admin-rebuild/03-数据与API重写.md)：数据库、RPC、API、service 层重写计划。
- [04-旧后台删除清单](docs/admin-rebuild/04-旧后台删除清单.md)：旧页面、旧组件、旧 lib、旧 API 的替换和删除顺序。
- [05-测试验收清单](docs/admin-rebuild/05-测试验收清单.md)：每阶段验收场景、命令、人工检查项。

## 当前执行原则

- 不新建 `admin-v2` 路由，直接演进 `src/app/[locale]/admin/*`。
- 新后台基础层统一放在 `src/admin/`，旧 `src/components/admin/*` 仅在迁移期保留。
- `/zh/admin` 为主体验；`/it/admin` 保留可访问、关键导航和状态文案。
- RMA 不恢复，退款和售后记录归入订单与财务模块。
- 每一阶段都必须保持 `npm run lint`、`npm run typecheck`、`npm run build` 可通过。
